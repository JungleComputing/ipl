IBIS:

- code formatting of entire tree
- IPL += sendPort.connect(ReceivePortIdentifier[] receivers);
  After distribution.
- ibis-* scripts (with consistent command line options)
- remove das detection from build system
- remove prun/wrapper/? support from run-ibis/ibis-run, move to new ibis-*
  scripts
- move bcel source to location equal to package name
- add native detection to timer
- add java 1.5 timer to ibis.util.timer


satin:
- add semantic analysis to the compiler
- forgotten sync statements
- make return / throw an implicit sync again?
- add comments to abort / inlet machinism
- als exception gegooid wordt zonder catch krijgen we nu aborts... / geen print bij top of the stack.


general:
- IBISC maken / fixen
  after distribution.
- run central nameserver on fs0
