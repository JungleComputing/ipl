apply from: "${rootProject.rootDir}/gradle/sub.gradle"
apply from: "${rootProject.rootDir}/gradle/sub-rewrite.gradle"

dependencies {
    implementation project(':api')
    implementation project(':util')
}

base {
    archivesName = 'ibis-benchmarks'
}    
