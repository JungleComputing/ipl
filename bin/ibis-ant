#!/bin/sh

if [ -z "$IBIS_HOME" ];  then
    echo "please set IBIS_HOME to the location of your Ibis installation" 1>&2
    exit 1
fi

if [ -z "$JAVA_HOME" ]
then
    echo "ibis-ant: the JAVA_HOME environment variable must be set" 1>&2
    exit 1
fi

export JAVA_HOME

ant "$@"
