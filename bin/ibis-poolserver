#!/bin/sh

# This script is a convenience script to start a poolserver.

# Check IBIS_HOME.
if [ -z "$IBIS_HOME" ];  then
    echo "please set IBIS_HOME to the location of your Ibis installation" 1>&2
    exit 1
fi

. $IBIS_HOME/bin/ibis-run ibis.util.PoolInfoServer "$@"
