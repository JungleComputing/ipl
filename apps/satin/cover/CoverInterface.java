public interface CoverInterface extends ibis.satin.Spawnable {
	public Return spawn_try_it(int i, int N, int no_skills, byte[][] skills,
		      int cover, byte[] covered, byte[] act, byte[] opt, int opt_elems);
}
