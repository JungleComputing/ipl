bladiebladiebla
bladiebladiebla
bladiebladiebla
bladiebladiebla
dit is een stuk random tekst waar hij zelf maar moet zien of er iets te
compremeren valt. Ik heb geen idee wat 'ie gaat vinden. Maar het zou
handig zijn als hij daarbij een fout zou maken.
    /** Apply one step in the folding process. */
    public boolean applyFolding() throws VerificationException
    {
        if( length == 0 ){
            return false;
        }
	int max = 0;
        int maxgain = 0;
        int repeats = 0;

	for( int i=1; i<length; i++ ){
            int r = 0;
            int gain = -1;

	    if( commonality[i]>2 ){
                r = 2;
                for( int j = i+1; j<length; j++ ){
                    if( commonality[j] == commonality[i] ){
                        r++;
                    }
                    else {
                        break;
                    }
                }
                gain = (r*(commonality[i]-1)) - (commonality[i]+1);
            }
	    if( gain>maxgain ){
		max = i;
                repeats = r;
                maxgain = gain;
	    }
	}

        if( maxgain>0 ){
            if( traceCompressionCosts ){
                System.out.println( "String [" + buildString( indices[max], commonality[max] ) + "] has " + repeats + " repeats: gain=" + maxgain );
            }
            applyCompression( max );
            if( doVerification ){
                test();
            }
        }
        return maxgain>0;
    }
