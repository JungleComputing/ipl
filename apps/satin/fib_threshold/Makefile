APP_OPTIONS = 52

MAIN = FibThreshold.java
OUT= fib_threshold

include ../../../config.mk
include ../config.mk

satin:
	$(IBISC_COMMAND) -no-aborts $(MAIN)

clean:
	rm -f $(TRASH_FILES)

include ../run.mk
