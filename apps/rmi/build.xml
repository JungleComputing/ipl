<project
    name="RMI applications"
    default="build"
    basedir=".">

    <description>
	RMI applications build.
    </description>

    <property environment="env"/>

    <property name="ibis"        location="${env.IBIS_HOME}"/>

    <import file="../build-apps.xml"/>

</project>
