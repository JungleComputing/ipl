<project
    name="ibis.apps.rmi.radix"
    default="build"
    basedir=".">

    <description>
	Ibis RMI application build.
    </description>

    <property name="main-class"  value="Radix"/>

    <property environment="env"/>

    <property name="ibis"        location="${env.IBIS_HOME}"/>
    <property name="build"       location="build"/>

    <property name="test-par-run" value="Radix -t -r64"/>
    <property name="test-seq-run" value="Radix -t -r64"/>

    <import file="${ibis}/build-files/apps/build-rmi-app.xml"/>

</project>


