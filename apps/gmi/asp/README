All-pairs Shortest Path (ASP).

This program computes the shortest path between ant two nodes of a given graph.

The program takes one argument: the number of rows (and columns) in the
distance table.
