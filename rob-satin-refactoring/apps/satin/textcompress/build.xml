<project
    name="ibis.apps.satin.sat"
    default="build"
    basedir=".">

    <property environment="env"/>

    <property name="ibis"        location="${env.IBIS_ROOT}"/>
    <property name="build"       value="build"/>

    <!--
    <property name="satinc-flags" value=""/>
    -->

    <property name="satinc-flags" value="-keep"/>

    <property name="main-class" value="Compress"/>
    <property name="satin-main-class" value="${main-class}"/>

    <import file="${ibis}/build-files/apps/build-satin-app.xml"/>

    <target name="test"
	    depends="compile"
	    description="Run Junit test">
	<antcall target="runseq">
		<param name="runjob" value="junit.textui.TestRunner Tests"/>
	</antcall>
    </target>

    <target name="docs"
        description="Build JavaDoc documentation in `docs' directory">
	<!-- Create the javadoc directory -->
	<delete dir="docs"/>
	<mkdir dir="docs"/>
	<javadoc destdir="docs" sourcepath="${ibis}/src" author="true" version="true" use="true" windowtitle="Compressor documentation" doctitle="Compressor Documentation" bottom="The Ibis project">
	<FileSet file="*.java"/>
	<classpath refid="default.classpath"/>
	</javadoc>
    </target>
</project>
