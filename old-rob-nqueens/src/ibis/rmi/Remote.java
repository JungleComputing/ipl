/* $Id$ */

package ibis.rmi;

/**
 * The <code>Remote</code> interface serves to identify interfaces whose
 * methods may be remotely invoked. Any remote object must implement
 * this interface. Only methods specified in an interface that
 * extends <code>ibis.rmi.Remote</code> are available remotely.
 */
public interface Remote {
    //magic interface
}